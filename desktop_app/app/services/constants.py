"""Shared UI/workflow constants (no framework imports)."""

# Send tree: synthetic ids for WhatsApp directory rows (avoid collision with local_contacts.id).
WA_SEND_ID_OFFSET = 8_000_000

SEND_TEMPLATE_CUSTOM = "(Custom — type message below)"
